# Capas de livros: leitura pública de imagens, sem execução de scripts.
<FilesMatch "\.(php|phtml|php\d)$">
    Require all denied
</FilesMatch>

<IfModule mod_php.c>
    php_flag engine off
</IfModule>

Options -Indexes
